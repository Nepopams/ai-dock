# Planning Sprints

Sprint layer is optional.

Create a sprint only when a release or delivery slice needs its own planning container, coordination boundary, acceptance criteria, and checklist. Do not create a sprint folder for every workpack.

The current Judge Mode / Evaluation Studio MVP uses roadmap slices inside `docs/planning/epics/EP-JUDGE-001-evaluation-studio-mvp/` and does not require a separate sprint folder.
