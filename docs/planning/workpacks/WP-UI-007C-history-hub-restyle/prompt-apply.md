# WP-UI-007C APPLY Prompt

Apply visual-only UI v2 styling to `HistoryView.tsx` and scoped History selectors in `global.css`. Preserve all search, reset, ingest, open-in-source, continue-in-chat, create-thread, auto-open, and store behavior.
