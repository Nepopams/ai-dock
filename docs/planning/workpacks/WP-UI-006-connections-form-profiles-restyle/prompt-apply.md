# Prompt Apply: WP-UI-006 Connections/Form Profiles Restyle

Apply only visual UI v2 changes inside allowed files. Preserve all existing event handlers, store usage, IPC calls, schemas, and runtime behavior. Use existing `--aid-*` tokens and scoped CSS. Do not add dependencies, libraries, or broad global rewrites.
