# Prompt FIXPACK - WP-UI-008

If REVIEW finds missing docs/assets or path drift, fix only the acceptance pack within the original allowed files. Do not perform UI runtime changes. Any real visual UI fix must be a later scoped WP-UI-009 workpack after screenshots and gap matrix rows exist.
