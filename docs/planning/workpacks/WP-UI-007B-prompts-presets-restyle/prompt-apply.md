# WP-UI-007B APPLY Prompt

Apply visual-only UI v2 styling to `TemplatesManager`, `InsertPromptDialog`, `PresetsGallery`, `ApplyPresetDialog`, and scoped `global.css` selectors. Preserve every existing handler, store action, template/preset schema, import/export semantic, adapter warning, and apply payload.
