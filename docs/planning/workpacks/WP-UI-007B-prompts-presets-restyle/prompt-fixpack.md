# WP-UI-007B FIXPACK Prompt

If REVIEW finds defects, make only scoped visual/CSS fixes inside allowed files. Do not change template/preset behavior, stores, shared contracts, IPC, dependencies, or unrelated screens.
