# WP-UI-009 Prompt REVIEW

Review the APPLY diff as a code reviewer.

Check:
- `ConfirmDialog` behavior unchanged.
- `KeyValueEditor` behavior unchanged.
- No screen-level restyle.
- No forbidden paths changed.
- Focus, danger, disabled, empty, warning, and readability states are visible.
- Tests/build/validators/diff checks pass.
- Manual smoke checklist is recorded.

Findings must lead with bugs or scope violations.

