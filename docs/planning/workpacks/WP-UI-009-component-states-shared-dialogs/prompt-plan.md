# WP-UI-009 Prompt PLAN

You are the `ai-dock-renderer-react-executor` for VR AI Dock.

Plan `WP-UI-009-component-states-shared-dialogs`.

Answer explicitly:
1. Which Component States Board export is available.
2. Which shared components still use old utility-like styling.
3. Which `ConfirmDialog` changes are visual-only.
4. Which `KeyValueEditor` changes are visual-only.
5. Whether `Toast` needs a change.
6. Which shared/global state CSS selectors will be touched.
7. How broad screen restyle is avoided.
8. Which exact files will be changed.
9. Whether any strong gate is active.

Allowed files are only the files listed in `workpack.md`.

