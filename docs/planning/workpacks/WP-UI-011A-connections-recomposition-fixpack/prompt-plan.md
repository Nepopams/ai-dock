# WP-UI-011A PLAN Prompt

Plan the Connections Recomposition Fixpack.

Must answer:
1. What visual evidence makes Connections NO-GO?
2. Which visible current UI strings prove `CompletionsSettings.tsx` owns the screen?
3. Why CSS-only is insufficient.
4. What target layout will be implemented.
5. Which existing behaviors remain unchanged.
6. Which JSX owner components and CSS sections will change.
7. How token redaction remains preserved.
8. How registry preview uses existing data without changing registry behavior.
9. Exact files changed.
10. Strong gate status.

Runtime APPLY is allowed only within the workpack allow-list and only if no schema, store, IPC, package, dependency, or token behavior changes are required.
