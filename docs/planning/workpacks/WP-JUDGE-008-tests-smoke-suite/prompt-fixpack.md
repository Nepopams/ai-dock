# Prompt: FIXPACK for WP-JUDGE-008 Tests and Smoke Suite

Use only if REVIEW finds a Must Fix inside the approved QA/docs/test scope.

Allowed fixpack changes:
- Correct incomplete QA docs.
- Correct missing required scenario headings or coverage references.
- Correct initiative/workpack validation sections.
- Correct the no-dependency docs QA test.

Stop and request a Human gate if the fix requires runtime source, dependency, package, IPC, preload, shared, renderer, storage, export, provider, or automation harness changes.
